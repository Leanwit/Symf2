{% extends 'FacturaBundle::layout.html.twig' %}

{% block body %}
<!-- Static navbar -->

<div class="container">
    <!-- Main component for a primary marketing message or call to action -->
    <div class="jumbotron">
        <h3>Proveedores</h3>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Descripcion</th>
                    <th>Direccion</th>
                    <th>Telefono</th>
                    <th>Email</th>
                    <th>Acción</th>
                </tr>
            </thead>
            <tbody>
        {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('proveedor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{{ entity.descripcion }}</td>
                        <td>{{ entity.direccion }}</td>
                        <td>{{ entity.telefono }}</td>
                        <td>{{ entity.email }}</td>
                        <td>
                            <li class="sinPunto">                                    
                                    <a class="btn btn-default glyphicon glyphicon-search" href="{{ path('proveedor_show', { 'id': entity.id }) }}"></a>
                                </li>
                                <li class="sinPunto">                                 
                                    <a class="btn btn-default glyphicon glyphicon-edit" href="{{ path('proveedor_edit', { 'id': entity.id }) }}"></a>                                    
                                </li>
                        </td>
                    </tr>
        {% endfor %}
                </tbody>
            </table>
          
        </div>
    </div> <!-- /container -->

    {% endblock %}
