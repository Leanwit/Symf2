{% extends 'FacturaBundle::layout.html.twig' %}

{% block body %}
<!-- Static navbar -->

<div class="container">
    <!-- Main component for a primary marketing message or call to action -->
    <div class="jumbotron">
        <h1>Lista de Proveedores</h1>

      <table class="table table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Descripcion</th>
                    <th>Direccion</th>
                    <th>Telefono</th>
                    <th>Email</th>
                    <th>Acción</th>
                </tr>
            </thead>
            <tbody>
        {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('proveedor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{{ entity.descripcion }}</td>
                        <td>{{ entity.direccion }}</td>
                        <td>{{ entity.telefono }}</td>
                        <td>{{ entity.email }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('proveedor_show', { 'id': entity.id }) }}">Ver</a>
                                </li>
                                <li>
                                    <a href="{{ path('proveedor_edit', { 'id': entity.id }) }}">Editar</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
        {% endfor %}
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('proveedor_new') }}">
                        Crear nuevo proveedor
                    </a>
                </li>
            </ul>
        </div>
    </div> <!-- /container -->

    {% endblock %}
