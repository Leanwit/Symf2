{% extends 'FacturaBundle::layout.html.twig' %}

{% block body %}
<!-- Static navbar -->

<div class="container">
    <!-- Main component for a primary marketing message or call to action -->
    <div class="jumbotron">
       <h3>Condicion Pago</h3>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
        </tbody>
    </table>
     <table>
                <tr>
                    <td><a class="btn btn-sm btn-primary" type="button" href="{{ path('condicionpago') }}">Volver al listado </a></td> 
                    <td><a class="btn btn-sm btn-primary" type="button" href="{{ path('condicionpago_edit', { 'id': entity.id }) }}">Editar</a></td>                    
                    <td>{{ form(delete_form)}}</td>
                </tr>
        </table>  
     
   </div>
</div> <!-- /container -->
    
{% endblock %}
